

const SectionTitle = ({ heading }) => {
    return (
        <div>
            <h3 className='text-center fw-bold fs-1' style={{color: '#f56981'}}>{heading}</h3>
        </div>
    );
};

export default SectionTitle;